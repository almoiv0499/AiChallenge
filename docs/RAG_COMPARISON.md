# Сравнение ответов с RAG и без RAG

## Описание

Реализована функция сравнения ответов модели с использованием RAG (Retrieval-Augmented Generation) и без него. Это позволяет оценить, насколько RAG улучшает качество ответов и в каких случаях он наиболее полезен.

## Как это работает

### Процесс сравнения

1. **Вопрос пользователя** → система получает запрос
2. **Поиск релевантных чанков** → RAG сервис ищет похожие документы в локальной базе данных
3. **Объединение с вопросом** → найденный контекст добавляется к запросу
4. **Запрос к LLM** → модель получает два запроса:
   - Без RAG контекста (только вопрос)
   - С RAG контекстом (вопрос + релевантные чанки)
5. **Сравнение ответов** → система выводит оба ответа и анализ различий

## Использование

### Команды

#### `/rag` - Переключение RAG режима
Включает или выключает использование RAG при обработке запросов.

```
/rag
```

**Пример:**
```
Вы: /rag
✅ RAG режим включен
```

#### `/rag-compare` - Режим сравнения
Включает или выключает режим сравнения. В этом режиме каждый вопрос обрабатывается дважды: с RAG и без RAG.

```
/rag-compare
```

**Пример:**
```
Вы: /rag-compare
✅ Режим сравнения RAG включен
   💡 Каждый вопрос будет обрабатываться дважды: с RAG и без RAG
```

### Пример использования

1. **Включите режим сравнения:**
   ```
   Вы: /rag-compare
   ✅ Режим сравнения RAG включен
   ```

2. **Задайте вопрос:**
   ```
   Вы: Что такое MCP stdio transport?
   ```

3. **Система автоматически:**
   - Обработает вопрос без RAG
   - Обработает вопрос с RAG
   - Выведет оба ответа и анализ

4. **Результат:**
   ```
   📊 СРАВНЕНИЕ ОТВЕТОВ: RAG vs БЕЗ RAG
   ============================================================
   
   ❓ Вопрос: Что такое MCP stdio transport?
   
   📚 Найденный RAG контекст:
   [1] Источник: docs.md
   Сходство: 0.85
   Текст: MCP stdio transport - это...
   
   ----------------------------------------
   ❌ ОТВЕТ БЕЗ RAG:
   ----------------------------------------
   MCP stdio transport - это механизм передачи данных...
   
   ----------------------------------------
   ✅ ОТВЕТ С RAG:
   ----------------------------------------
   MCP stdio transport - это механизм передачи данных через стандартный ввод/вывод...
   
   ============================================================
   🔍 АНАЛИЗ:
   ============================================================
   📏 Длина ответов:
      Без RAG: 150 символов
      С RAG: 250 символов
      Разница: +100 символов
   
   📝 Количество слов:
      Без RAG: 25 слов
      С RAG: 40 слов
      Разница: +15 слов
   
   🔗 Схожесть ответов: 65.0%
   
   💡 Выводы:
      ✅ RAG значительно изменил ответ - контекст был релевантным
      📚 Ответ с RAG значительно подробнее - добавлена информация из базы знаний
   ```

## Анализ результатов

### Метрики сравнения

1. **Длина ответов** - сравнение количества символов
2. **Количество слов** - сравнение объема текста
3. **Схожесть ответов** - процент общих слов (Jaccard similarity)
4. **Выводы** - автоматический анализ влияния RAG

### Интерпретация результатов

#### RAG помог (схожесть < 50%)
- ✅ Контекст был релевантным
- ✅ Ответ стал более точным и информативным
- ✅ Добавлена специфическая информация из базы знаний

#### RAG частично помог (схожесть 50-80%)
- ⚠️ Контекст был частично релевантным
- ⚠️ Ответ изменился, но незначительно
- ⚠️ Возможно, нужны более релевантные документы

#### RAG не помог (схожесть > 80%)
- ℹ️ Контекст был не очень релевантным
- ℹ️ Ответ почти не изменился
- ℹ️ Возможно, вопрос не требует дополнительного контекста

## Когда RAG наиболее полезен

### ✅ RAG помогает, когда:
- Вопрос касается специфической информации из проиндексированных документов
- Нужны точные детали, которые могут быть не в обучающих данных модели
- Требуется актуальная информация из локальной базы знаний
- Вопрос требует контекста из конкретных документов проекта

### ❌ RAG не помогает, когда:
- Вопрос общий и не требует специфического контекста
- В базе знаний нет релевантной информации
- Вопрос касается общих знаний, которые модель уже знает
- Контекст не найден (similarity < порога)

## Настройка

### Параметры RAG

В `RagService` можно настроить:
- `minSimilarity` (по умолчанию 0.6) - минимальный порог сходства
- `maxChunks` (по умолчанию 3) - максимальное количество чанков

### Пороги анализа

В `ConsoleUI.analyzeDifferences()`:
- Схожесть < 50% → RAG значительно помог
- Схожесть 50-80% → RAG частично помог
- Схожесть > 80% → RAG мало помог

## Примеры тестирования

### Тест 1: Вопрос с релевантным контекстом
```
Вопрос: "Что такое MCP stdio transport?"
Ожидаемый результат: RAG должен значительно улучшить ответ
```

### Тест 2: Общий вопрос
```
Вопрос: "Что такое искусственный интеллект?"
Ожидаемый результат: RAG может не найти релевантный контекст
```

### Тест 3: Специфический вопрос по документации
```
Вопрос: "Как работает Android emulator agent?"
Ожидаемый результат: RAG должен найти релевантные чанки и улучшить ответ
```

## Выводы

Режим сравнения позволяет:
1. **Оценить эффективность RAG** для конкретных типов вопросов
2. **Найти оптимальные пороги** similarity для вашей базы знаний
3. **Улучшить качество индексации** документов
4. **Понять, когда RAG действительно помогает**, а когда нет

## Рекомендации

1. **Используйте режим сравнения** для тестирования новых документов
2. **Анализируйте метрики** для оптимизации параметров RAG
3. **Индексируйте релевантные документы** для лучших результатов
4. **Настраивайте пороги similarity** в зависимости от ваших данных




