# Реализация интеграции RAG для Project Task API

## Что было реализовано

### 1. Автоматическое определение релевантности запросов

**Функция:** `isProjectTaskRelatedQuery(message: String)`

Определяет, связан ли запрос пользователя с задачами проекта по ключевым словам:
- задача, задачи, проект, статус, приоритет
- дедлайн, релиз, milestone, критичн, блокер
- исполнитель, assignee, epic, просрочен, overdue
- команда, team, capacity, загрузка, workload

**Примеры:**
- ✅ "Какие задачи критичны?" → `true`
- ✅ "Покажи статус проекта" → `true`
- ✅ "Загрузка команды" → `true`
- ❌ "Какая погода?" → `false`

---

### 2. Получение RAG контекста проекта

**Функция:** `getProjectContext(query: String)`

Ищет релевантный контекст в документации проекта через RAG:

1. **Первичный поиск:** Ищет по запросу пользователя (limit=3, minSimilarity=0.5)
2. **Fallback поиск:** Если ничего не найдено, ищет общий контекст проекта
3. **Форматирование:** Возвращает отформатированный контекст с указанием источника

**Пример результата:**
```
README.md:
Проект находится на этапе MVP. Релиз запланирован на 15 февраля 2025.
Критичные компоненты: интеграция платежей, авторизация пользователей.

ROADMAP.md:
Текущие приоритеты:
1. Завершение интеграции платежей (критично)
2. Исправление багов авторизации
3. Оптимизация производительности
```

---

### 3. Интеграция в processMessage

**Изменения в:** `processMessage(userMessage: String)`

Теперь при обработке сообщения:

1. Проверяется, связан ли запрос с задачами проекта
2. Если да и RAG доступен → получается контекст проекта
3. Контекст добавляется к сообщению пользователя
4. Агент получает обогащенное сообщение с контекстом

**Пример:**
```
Пользователь: "Какие задачи критичны для релиза?"

Обработка:
1. isProjectTaskRelatedQuery() → true
2. getProjectContext() → находит информацию о релизе
3. Обогащенное сообщение:
   "Контекст проекта (из документации):
    Релиз MVP запланирован на 15 февраля 2025...
    
    Вопрос пользователя: Какие задачи критичны для релиза?"
4. Агент использует контекст + API для ответа
```

---

### 4. Контекст проекта в системном промпте

**Функция:** `addProjectContextToSystemPrompt()`

При инициализации агента:

1. Выполняет поиск по общим темам:
   - "проект статус этап roadmap"
   - "дедлайн релиз milestone"
   - "команда роли участники"

2. Добавляет найденный контекст в системный промпт

3. Агент всегда имеет базовый контекст проекта

**Вызывается:**
- При инициализации агента (`init`)
- При очистке истории (`clearHistory`)

---

## Как это работает

### Сценарий 1: Запрос о задачах проекта

```
Пользователь: "Какие задачи критичны для релиза?"

Шаг 1: Определение релевантности
  isProjectTaskRelatedQuery("Какие задачи критичны для релиза?")
  → true (найдены: "задачи", "критичн", "релиз")

Шаг 2: Получение RAG контекста
  getProjectContext("Какие задачи критичны для релиза?")
  → Ищет в документации: "релиз", "критические задачи"
  → Находит: "Релиз MVP запланирован на 15 февраля. Критичные задачи..."

Шаг 3: Обогащение сообщения
  Обогащенное сообщение отправляется агенту

Шаг 4: Обработка агентом
  Агент:
  1. Видит контекст проекта из документации
  2. Вызывает get_project_tasks с priority=CRITICAL
  3. Вызывает get_project_status
  4. Формирует ответ с учетом контекста:
     "Для релиза MVP (15 февраля) критичны следующие задачи..."
```

---

### Сценарий 2: Запрос без RAG контекста

```
Пользователь: "Какая погода в Москве?"

Шаг 1: Определение релевантности
  isProjectTaskRelatedQuery("Какая погода в Москве?")
  → false (нет ключевых слов)

Шаг 2: Пропуск RAG
  Сообщение обрабатывается как обычно

Шаг 3: Обработка агентом
  Агент вызывает get_weather для Москвы
```

---

### Сценарий 3: Запрос с общим контекстом из системного промпта

```
Пользователь: "Покажи статус проекта"

Шаг 1: Определение релевантности
  → true

Шаг 2: Получение RAG контекста
  → Может найти дополнительный контекст

Шаг 3: Обработка
  Агент имеет:
  - Базовый контекст из системного промпта (добавлен при старте)
  - Дополнительный контекст из текущего запроса
  - Данные из get_project_status API
  
  Формирует ответ с учетом всего контекста
```

---

## Преимущества

### 1. Автоматизация
- Не нужно явно запрашивать контекст через `/help`
- RAG работает автоматически для релевантных запросов

### 2. Контекстуальные ответы
- Агент знает о проекте из документации
- Может учитывать roadmap, дедлайны, приоритеты

### 3. Умные рекомендации
- Понимает бизнес-логику проекта
- Может давать обоснованные рекомендации по приоритетам

### 4. Точность
- Ответы основаны на актуальной документации + данных из API
- Комбинация RAG + API дает полную картину

---

## Примеры работы

### Без RAG (старое поведение):
```
Пользователь: "Какие задачи критичны для релиза?"
Агент: [get_project_tasks priority=CRITICAL]
       "Найдено 5 критических задач:
        1. Интеграция платежей
        2. Исправление багов..."
       ❌ Не знает, что такое "релиз" в контексте проекта
```

### С RAG (новое поведение):
```
Пользователь: "Какие задачи критичны для релиза?"
Агент: [RAG находит: "Релиз MVP запланирован на 15 февраля"]
       [get_project_tasks priority=CRITICAL]
       [get_project_status]
       "Для релиза MVP (15 февраля 2025) критичны следующие задачи:
        1. Интеграция платежей - блокирует другие задачи
        2. Исправление багов авторизации - критично для безопасности
        ...
        
        Статус: 3 из 5 критических задач в работе, 2 просрочены.
        Рекомендация: сфокусироваться на просроченных задачах."
       ✅ Знает контекст проекта и дает умные рекомендации
```

---

## Настройка

### Пороги поиска

В функции `getProjectContext()`:
- **Первичный поиск:** `minSimilarity = 0.5` (строгий)
- **Fallback поиск:** `minSimilarity = 0.3` (мягкий)
- **Лимит результатов:** `limit = 3` (первичный), `limit = 2` (fallback)

Можно настроить под ваши нужды.

### Ключевые слова

В функции `isProjectTaskRelatedQuery()` можно добавить свои ключевые слова для определения релевантности.

### Контекст при старте

В функции `addProjectContextToSystemPrompt()` можно изменить запросы для поиска общего контекста.

---

## Отладка

### Логирование

При работе RAG выводится:
- `📚 Найден контекст проекта через RAG` - когда контекст найден
- `📚 Контекст проекта добавлен в системный промпт` - при инициализации
- `⚠️ Ошибка при получении RAG контекста` - при ошибках

### Проверка работы

1. Запустите приложение
2. Проверьте, что выводится: `📚 Контекст проекта добавлен в системный промпт`
3. Задайте вопрос о задачах проекта
4. Проверьте, что выводится: `📚 Найден контекст проекта через RAG`

---

## Ограничения

1. **RAG работает только если:**
   - Документация проиндексирована
   - `ragService` инициализирован
   - Есть релевантная информация в документации

2. **Производительность:**
   - RAG поиск добавляет небольшую задержку (~100-500ms)
   - Используется только для релевантных запросов

3. **Точность:**
   - Зависит от качества индексации документации
   - Зависит от релевантности документации запросу

---

## Следующие шаги

1. ✅ Интеграция RAG в processMessage
2. ✅ Добавление контекста в системный промпт
3. 🔄 Тестирование на реальных запросах
4. 🔄 Настройка порогов и ключевых слов
5. 🔄 Оптимизация производительности

---

## Итог

Теперь RAG автоматически используется для:
- ✅ Получения контекста проекта при запросах о задачах
- ✅ Добавления общего контекста в системный промпт
- ✅ Обогащения ответов агента информацией из документации

Агент стал умнее и может давать контекстуальные ответы, основанные на документации проекта! 🚀
