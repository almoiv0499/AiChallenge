# Интеграция RAG (Retrieval-Augmented Generation)

## Описание

Система теперь использует **RAG (Retrieval-Augmented Generation)** для поиска информации в локальной базе данных документов перед отправкой запроса в OpenRouter API.

## Как это работает

1. **Пользователь задает вопрос** в чате
2. **RAG сервис** автоматически:
   - Генерирует эмбеддинг для запроса пользователя
   - Ищет похожие документы в локальной SQLite базе данных
   - Находит топ-3 наиболее релевантных чанка (по умолчанию)
3. **Найденный контекст** добавляется как системное сообщение перед запросом пользователя
4. **LLM** получает и запрос пользователя, и релевантный контекст из базы знаний
5. **Ответ** формируется на основе локальной информации + знаний модели

## Настройка

### 1. Индексация документов

Перед использованием RAG необходимо проиндексировать документы:

```bash
gradlew runIndexDocs
```

Это создаст базу данных `document_index.db` с эмбеддингами документов.

### 2. Автоматическая активация

RAG автоматически активируется при запуске `main()`, если:
- ✅ База данных `document_index.db` существует
- ✅ В базе есть проиндексированные документы
- ✅ API ключ OpenRouter настроен

При запуске вы увидите:
```
✅ RAG service initialized (local document search enabled)
```

Или, если индекс пуст:
```
⚠️ RAG service initialized but no documents in index. Run 'gradlew runIndexDocs' to index documents.
```

## Использование

После индексации документов просто задавайте вопросы в чате. Система автоматически:

1. Определит, есть ли релевантная информация в локальной БД
2. Если найдена - добавит контекст к запросу
3. Ответит на основе локальной информации

### Примеры запросов

Если вы проиндексировали `docs/docs.md`, вы можете спросить:

- "Что такое MCP stdio transport?"
- "Как работает Android emulator agent?"
- "Расскажи про архитектуру проекта"
- "Какие этапы разработки описаны в документации?"

Система найдет релевантные фрагменты из `docs/docs.md` и использует их для ответа.

## Параметры RAG

В `RagService` можно настроить:

- `minSimilarity` (по умолчанию 0.6) - минимальный порог сходства для включения чанка
- `maxChunks` (по умолчанию 3) - максимальное количество чанков для контекста

## Преимущества

✅ **Быстрый поиск** - поиск в локальной БД быстрее, чем в интернете  
✅ **Актуальная информация** - использует ваши собственные документы  
✅ **Контроль данных** - полный контроль над источниками информации  
✅ **Работа offline** - поиск работает без интернета (кроме генерации эмбеддингов запросов)  
✅ **Точность** - семантический поиск находит релевантную информацию даже при неполном совпадении слов

## Ограничения

⚠️ Для генерации эмбеддингов запросов все еще требуется доступ к OpenRouter API  
⚠️ Если в индексе нет релевантной информации, система продолжит работу без RAG контекста  
⚠️ RAG работает только с проиндексированными документами

## Отключение RAG

Если RAG не инициализирован (нет документов в индексе), система продолжит работу в обычном режиме, используя только знания LLM.

## Логирование

При успешном поиске контекста вы увидите:
```
📚 Найден релевантный контекст в локальной базе знаний
```

Это означает, что система нашла релевантную информацию и использует ее для ответа.

