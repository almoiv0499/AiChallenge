# Android Chat AI Application

Android-приложение для чата с AI ассистентом на базе OpenRouter API.

## 🏗️ Архитектура

- **MVVM** - Model-View-ViewModel паттерн
- **Jetpack Compose** - современный UI framework
- **Room** - локальная база данных для хранения истории
- **Koin** - Dependency Injection
- **Ktor** - сетевые запросы
- **Flow / StateFlow** - реактивное программирование
- **Coroutines** - асинхронные операции

## 📦 Структура проекта

```
android/
├── app/                    # Основной модуль приложения (UI, DI)
└── chatAI/                 # Модуль бизнес-логики (независим от Android)
```

## 🚀 Сборка проекта локально

### Требования

- **Android Studio** Hedgehog (2023.1.1) или новее
- **JDK 17**
- **Android SDK** (minSdk 24, targetSdk 34)
- **Gradle 8.5+**

### Настройка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd AiChallenge
```

2. **Настройте API ключ OpenRouter:**

Создайте файл `android/app/src/main/res/values/api_keys.xml` (не коммитьте его):
```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="openrouter_api_key">sk-or-v1-ваш_ключ</string>
</resources>
```

Или установите переменную окружения:
```bash
# Linux/macOS
export OPENROUTER_API_KEY=sk-or-v1-ваш_ключ

# Windows
set OPENROUTER_API_KEY=sk-or-v1-ваш_ключ
```

3. **Соберите проект:**
```bash
# Linux/macOS
cd android
./gradlew :app:assembleDebug

# Windows
cd android
gradlew.bat :app:assembleDebug
```

APK будет находиться в: `android/app/build/outputs/apk/debug/app-debug.apk`

4. **Установите на устройство:**
```bash
./gradlew :app:installDebug
```

## 📥 Скачивание APK из CI

После успешного прохождения GitHub Actions workflow:

1. Перейдите в раздел **Actions** в GitHub репозитории
2. Выберите последний успешный workflow run
3. Прокрутите вниз до раздела **Artifacts**
4. Скачайте один из артефактов:
   - **app-debug-apk** - APK файл напрямую
   - **app-debug-zip** - ZIP архив с APK

### Альтернативный способ

1. Откройте репозиторий на GitHub
2. В правом верхнем углу нажмите на **Actions**
3. В списке workflow runs найдите последний успешный
4. В правой части страницы найдите раздел **Artifacts**
5. Нажмите на нужный артефакт для скачивания

## 📱 Использование приложения

1. **Запустите приложение** на устройстве или эмуляторе
2. **Введите сообщение** в поле ввода внизу экрана
3. **Нажмите кнопку отправки** (иконка самолета)
4. **Дождитесь ответа** от AI ассистента
5. **История чата** автоматически сохраняется и восстанавливается при перезапуске

## 🔧 Настройка CI/CD

### GitHub Secrets

Для работы CI/CD с API ключом (опционально):

1. Перейдите в **Settings → Secrets and variables → Actions**
2. Нажмите **New repository secret**
3. Name: `OPENROUTER_API_KEY`
4. Value: ваш API ключ OpenRouter
5. Нажмите **Add secret**

Примечание: CI/CD будет работать и без этого ключа, но приложение может не работать без правильной настройки API ключа.

## 🗄️ База данных

История чата хранится в Room базе данных:

- **Таблица:** `chat_messages`
- **Поля:** `id`, `role`, `content`, `timestamp`
- **Роли:** `SYSTEM`, `USER`, `ASSISTANT`, `TOOL`, `MCP`
- **Восстановление:** автоматически при запуске приложения

## 📝 Особенности реализации

### Модульность

- **Модуль `chatAI`** полностью независим от Android UI
- Можно переиспользовать в других проектах
- Легко тестировать бизнес-логику

### Реактивность

- Использование **Flow** для автоматического обновления UI
- **StateFlow** для состояния в ViewModel
- Нет необходимости в ручном обновлении списка сообщений

### Обработка ошибок

- Использование `Result<T>` в UseCase
- Отображение ошибок через Snackbar
- Graceful degradation при сетевых ошибках

## 🐛 Troubleshooting

### Ошибка: "OPENROUTER_API_KEY не настроен"

**Решение:** Убедитесь, что API ключ установлен в `api_keys.xml` или в переменной окружения.

### Ошибка сборки: "SDK not found"

**Решение:** Установите Android SDK через Android Studio SDK Manager.

### CI/CD не собирает проект

**Решение:** 
- Проверьте, что workflow файл находится в `.github/workflows/`
- Убедитесь, что JDK 17 настроен правильно
- Проверьте логи в GitHub Actions

## 📚 Дополнительная документация

- [Архитектура и реализация](docs/ANDROID_IMPLEMENTATION.md)
- [API Reference](docs/API_REFERENCE.md)
- [Style Guide](docs/STYLE_GUIDE.md)

## 🔄 Обновления

### v1.0.0 (Текущая версия)
- Базовая функциональность чата
- Room для хранения истории
- Интеграция с OpenRouter API
- GitHub Actions CI/CD
