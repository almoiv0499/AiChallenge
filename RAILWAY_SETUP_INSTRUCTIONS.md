# 🚨 ТОЧНЫЕ ИНСТРУКЦИИ: Настройка Railway

## Проблема 1: Ошибка авторизации в CI

### Решение:

1. **Получите правильный токен Railway:**
   - Откройте [Railway Dashboard](https://railway.app/)
   - Перейдите в [Account Settings → Tokens](https://railway.app/account/tokens)
   - Нажмите **"New Token"**
   - Скопируйте токен (он начинается с `rw_`)

2. **Добавьте токен в GitHub Secrets:**
   - Откройте ваш репозиторий на GitHub
   - Перейдите в `Settings` → `Secrets and variables` → `Actions`
   - Нажмите **"New repository secret"**
   - **Name:** `RAILWAY_TOKEN`
   - **Value:** вставьте скопированный токен
   - Нажмите **"Add secret"**

3. **Проверьте другие секреты:**
   Убедитесь, что установлены:
   - `RAILWAY_TOKEN` - токен из шага 1
   - `RAILWAY_PROJECT_ID` - ID проекта (можно найти в URL проекта в Railway)
   - `RAILWAY_SERVICE_ID` - ID сервиса (можно найти в URL сервиса в Railway)

---

## Проблема 2: Переменная OPENROUTER_API_KEY не найдена на Railway

### ⚠️ ВАЖНО: Это нужно сделать СЕЙЧАС вручную!

### Решение (выберите один способ):

### Способ 1: Через Railway Dashboard (РЕКОМЕНДУЕТСЯ - самый быстрый)

1. Откройте [Railway Dashboard](https://railway.app/)
2. Выберите ваш проект
3. Выберите ваш сервис (service)
4. Перейдите в раздел **"Variables"** (вкладка слева)
5. Нажмите **"+ New Variable"**
6. Заполните:
   - **Name:** `OPENROUTER_API_KEY`
   - **Value:** ваш ключ OpenRouter API (начинается с `sk-or-v1-`)
7. Нажмите **"Add"**
8. Railway автоматически перезапустит сервис

**Готово!** Приложение должно заработать через 10-30 секунд.

### Способ 2: Через GitHub Secrets (для автоматической установки)

1. Откройте ваш репозиторий на GitHub
2. Перейдите в `Settings` → `Secrets and variables` → `Actions`
3. Нажмите **"New repository secret"**
4. **Name:** `OPENROUTER_API_KEY`
5. **Value:** ваш ключ OpenRouter API
6. Нажмите **"Add secret"**
7. Запустите workflow вручную или сделайте новый коммит

**Примечание:** Workflow установит переменную автоматически при следующем деплое.

---

## Проверка

### Проверка 1: Переменные в Railway

1. Откройте Railway Dashboard
2. Проект → Сервис → **Variables**
3. Убедитесь, что видите `OPENROUTER_API_KEY` в списке

### Проверка 2: Логи приложения

1. В Railway Dashboard откройте ваш сервис
2. Перейдите в **"Deployments"**
3. Откройте последний деплой
4. Проверьте логи - ошибка `OPENROUTER_API_KEY не найден` должна исчезнуть

### Проверка 3: CI/CD

1. Откройте GitHub → **Actions**
2. Найдите последний запуск workflow
3. Проверьте, что шаг "Deploy to Railway" выполнился успешно

---

## Если ничего не помогло

### Дополнительная проверка:

1. **Проверьте имя переменной:**
   - Должно быть точно: `OPENROUTER_API_KEY` (заглавными буквами)
   - Не `openrouter_api_key` или `OpenRouter_Api_Key`

2. **Проверьте, что переменная установлена для правильного сервиса:**
   - В Railway может быть несколько сервисов
   - Убедитесь, что переменная установлена для того сервиса, где запущено приложение

3. **Перезапустите сервис вручную:**
   - Railway Dashboard → Сервис → **"Redeploy"**

4. **Проверьте токен Railway:**
   - Убедитесь, что токен не истек
   - Создайте новый токен, если нужно

---

## Быстрая проверка всех настроек

✅ **GitHub Secrets настроены:**
- `RAILWAY_TOKEN` = токен из Railway Dashboard
- `RAILWAY_PROJECT_ID` = ID проекта
- `RAILWAY_SERVICE_ID` = ID сервиса
- `OPENROUTER_API_KEY` = ваш ключ OpenRouter

✅ **Railway Variables настроены:**
- `OPENROUTER_API_KEY` = ваш ключ OpenRouter (установлен в Dashboard)

✅ **Сервис перезапущен:**
- После установки переменных Railway автоматически перезапускает сервис
- Или нажмите "Redeploy" вручную

---

**После выполнения этих шагов приложение должно заработать!**
