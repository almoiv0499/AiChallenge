# 🧪 Тестирование деплоя

Этот файл содержит инструкции по проверке работы пайплайнов деплоя.

## Быстрая проверка

### 1. Обновите версию

Измените файл `VERSION` для триггера пайплайна:

```bash
echo "1.0.1" > VERSION
git add VERSION
git commit -m "Test deployment pipeline"
git push origin main
```

### 2. Проверьте GitHub Actions

1. Откройте вкладку **Actions** в GitHub репозитории
2. Убедитесь, что workflow `Deploy to Railway` запустился
3. Проверьте логи выполнения

### 3. Проверьте деплой

После успешного деплоя проверьте endpoints:

#### Health Check
```bash
curl http://ваш-домен.railway.app/api/health
```

Ожидаемый ответ:
```json
{
  "status": "ok",
  "service": "Project Task API"
}
```

#### Deployment Test
```bash
curl http://ваш-домен.railway.app/api/deployment/test
```

Ожидаемый ответ:
```json
{
  "status": "success",
  "message": "Deployment test endpoint is working!",
  "version": "1.0.1",
  "timestamp": 1234567890,
  "deployment": {
    "platform": "Railway",
    "status": "active"
  }
}
```

#### Version Info
```bash
curl http://ваш-домен.railway.app/api/version
```

Ожидаемый ответ:
```json
{
  "version": "1.0.1",
  "application": "OpenRouter Agent",
  "deployment": "Railway"
}
```

## Автоматическое тестирование

### Использование скриптов

#### Linux/macOS
```bash
chmod +x scripts/test-deployment.sh
./scripts/test-deployment.sh http://ваш-домен.railway.app
```

#### Windows PowerShell
```powershell
.\scripts\test-deployment.ps1 -Url "http://ваш-домен.railway.app"
```

### GitHub Actions Workflow

Используйте workflow `Test Deployment`:

1. Перейдите в **Actions** -> **Test Deployment**
2. Нажмите **Run workflow**
3. Укажите URL вашего деплоя
4. Запустите workflow

Или он автоматически запустится каждый день в 2:00 UTC.

## Проверка локально

Если вы хотите проверить endpoints локально:

```bash
# Запустите приложение
./gradlew run

# В другом терминале проверьте endpoints
curl http://localhost:8080/api/health
curl http://localhost:8080/api/deployment/test
curl http://localhost:8080/api/version
```

## Что проверяется

✅ **Health Check** - базовая работоспособность приложения  
✅ **Deployment Test** - проверка информации о деплое  
✅ **Version Info** - проверка версии приложения  

## Устранение проблем

### Endpoint не отвечает

1. Проверьте, что приложение запущено
2. Проверьте логи в Railway Dashboard
3. Убедитесь, что порт правильный (Railway использует переменную `PORT`)

### Ошибка 404

Убедитесь, что:
- Endpoints правильно зарегистрированы в приложении
- Сервер запущен и слушает правильный порт
- URL правильный (включая `/api/` префикс)

### Ошибка 500

Проверьте:
- Логи приложения в Railway
- Переменные окружения установлены правильно
- Зависимости загружены корректно

---

**Совет:** Обновляйте `VERSION` файл при каждом релизе для отслеживания версий и триггера деплоя!
