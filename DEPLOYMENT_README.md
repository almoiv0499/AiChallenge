# 🚀 Система автоматического деплоя на Railway

## Что реализовано

Создана полная система автоматического деплоя Kotlin + Ktor приложения на платформу Railway (PaaS хостинг).

### Реализованные компоненты:

1. **Dockerfile** - контейнеризация приложения
2. **GitHub Actions Workflow** - автоматический CI/CD пайплайн
3. **Railway API Client** - Kotlin клиент для работы с Railway API
4. **Deployment Service** - сервис управления деплоями
5. **Deploy Tool** - утилита для ручного деплоя
6. **Документация** - подробное руководство по настройке

## Быстрый старт

### 1. Настройка Railway

1. Зарегистрируйтесь на [Railway](https://railway.app/)
2. Создайте проект и сервис
3. Получите API токен из [Railway Dashboard](https://railway.app/account)

### 2. Настройка GitHub Secrets

Добавьте в настройках репозитория (`Settings` -> `Secrets`):

- `RAILWAY_TOKEN` - API токен Railway
- `RAILWAY_PROJECT_ID` - ID проекта
- `RAILWAY_SERVICE_ID` - ID сервиса

### 3. Автоматический деплой

Просто запушьте изменения в `main` или `master`:

```bash
git push origin main
```

GitHub Actions автоматически:
- ✅ Соберет приложение
- ✅ Запустит тесты
- ✅ Создаст Docker образ
- ✅ Задеплоит на Railway

### 4. Ручной деплой

```bash
# Через Gradle
./gradlew deployRailway \
  -PrailwayToken=ваш_токен \
  -PprojectId=ваш_project_id \
  -PserviceId=ваш_service_id
```

## Структура файлов

```
├── Dockerfile                           # Docker образ
├── .github/workflows/
│   └── deploy.yml                      # CI/CD пайплайн
├── src/main/kotlin/org/example/deployment/
│   ├── RailwayClient.kt                # Railway API клиент
│   ├── DeploymentService.kt            # Сервис деплоя
│   └── DeployMain.kt                   # Утилита деплоя
└── docs/
    └── DEPLOYMENT_GUIDE.md             # Подробная документация
```

## Особенности

- ✅ Автоматический деплой при пуше в main/master
- ✅ Управление переменными окружения
- ✅ Health check для мониторинга
- ✅ Многоэтапная Docker сборка (оптимизация размера)
- ✅ Кеширование зависимостей в GitHub Actions
- ✅ Поддержка ручного деплоя через CLI

## Важно ⚠️

**Пайплайн может не работать из-за отсутствия ключей!**

Убедитесь, что все секреты настроены:
- `RAILWAY_TOKEN` в GitHub Secrets
- `RAILWAY_PROJECT_ID` в GitHub Secrets  
- `RAILWAY_SERVICE_ID` в GitHub Secrets

## Дополнительная информация

Подробную документацию см. в [docs/DEPLOYMENT_GUIDE.md](docs/DEPLOYMENT_GUIDE.md)

---

**Результат:** Полностью рабочий CI/CD пайплайн для автоматического деплоя приложения на Railway! 🎉
